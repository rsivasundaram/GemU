#include "dev/myDevice.hh"

myDevice::myDevice(const Params *p, Addr pio_size): BasicPioDevice(p, pio_size), devname(p->devicename)
	{
		
		//int *ptr;
		//ptr= (int *) malloc(10*sizeof(int));
	}




